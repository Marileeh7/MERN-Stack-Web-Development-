import React from "react"; // Importa la biblioteca React

const Empty = () => {
  return <h2 className="nothing flex-center">Nothing to show</h2>; // Renderiza un encabezado cuando no hay nada que mostrar
};

export default Empty; // Exporta el componente Empty como el valor por defecto de este módulo
